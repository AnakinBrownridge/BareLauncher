name: Android CI

on:
  push:
    branches: [ "BareLauncher-main" ]
  pull_request:
    branches: [ "BareLauncher-main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: set up JDK 27
      uses: actions/setup-java@v4
      with:
        java-version: '27'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build
